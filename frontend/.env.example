VITE_BACKEND_URL=http://localhost:5000
VITE_GOOGLE_CLIENT_ID=your_google_client_id
VITE_PORT=5173
VITE_FIRST_TIME_USER_EMAIL=your_email@email.com
# VITE_FORCE_PROFILE_CREATION=false

# Note: login method visibility is controlled server-side via /auth/check.
# The backend ENABLE_GOOGLE_SSO and ENABLE_EMAIL_PASSWORD_AUTH env vars are the source of truth.